<nav class="navbar navbar-inverse bg-inverse navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="home-btn" href="/"><i class="fa fa-home" aria-hidden="true"></i></a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Agents <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <%= link_to "Transactions", transactions_path, class: 'nav-link' %>
      </li>
    </ul>
  </div>
</nav>

<section class="inner-padding">
  <button type="button" class="btn btn-primary app-btn" data-toggle="modal" data-target="#createModal">
    <i class="fa fa-plus" aria-hidden="true"></i></button>
  <input type="text" class="app-search" id="light-table-filter-agent" placeholder="Search.." data-table="order-table">
  <table class="app-table order-table table fixed_headers scroll">
    <thead>
    <tr class="header">
      <td class="bold">#</td>
      <td class="bold">Account</td>
      <td class="bold">Branch</td>
      <td class="bold">Created</td>
      <td class="bold">Updated</td>
      <td class="bold">Actions</td>
    </tr>
    </thead>
    <tbody>
    <% @agents.each_with_index do |agent, index| %>
        <tr>
          <td scope="row" class="bold"><%= index %></td>
          <td><%= agent.account %></td>
          <td><%= agent.branch %></td>
          <td><%= agent.created_at %></td>
          <td><%= agent.updated_at %></td>
          <td>
            <%= link_to '<div class="delete-btn"><i class="fa fa-times red-text"></i>&nbsp;Delete</div>'.html_safe, agent, method: :delete, data: {confirm: 'Are you sure?'} %>
          </td>
          <td></td>
        </tr>
    <% end %>
    </tbody>
  </table>

  <table class="app-table">
    <thead>
    <tr class="header">
      <td class="bold">Summary</td>
    </tr>
    </thead>
    <tbody>
    <tr>
      <td class="bold">Currently listing count</td>
      <td>:</td>
      <td id="listingCount"><%= @agents_count %></td>
    </tr>
    </tbody>
  </table>

</section>


<div class="modal fade" id="createModal" tabindex="-1" role="dialog" aria-labelledby="createModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="createModalLabel">Create a new Agent</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= form_for(@agent_item) do |f| %>
            <div class="field">
              <%= f.label :account %>
              <%= f.text_field :account, class: 'form-input' %>
            </div>
            <div class="field">
              <%= f.label :branch %>
              <%= f.text_field :branch, class: 'form-input' %>
            </div>

            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <%= f.submit :class => "btn btn-primary" %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


<div class="modal fade" id="updateModal" tabindex="-1" role="dialog" aria-labelledby="updateModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="createModalLabel">Create a new Agent</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= form_for(@agent_item) do |f| %>
            <div class="field">
              <%= f.label :account %>
              <%= f.text_field :account, class: 'form-input' %>
            </div>
            <div class="field">
              <%= f.label :branch %>
              <%= f.text_field :branch, class: 'form-input' %>
            </div>

            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <%= f.submit :class => "btn btn-primary" %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<style>
  .scroll td:nth-child(1) {
    flex: 1;
  }

  .scroll td:nth-child(2) {
    flex: 2;
  }

  .scroll td:nth-child(3) {
    flex: 2;
  }

  .scroll td:nth-child(4) {
    flex: 3;
  }

  .scroll td:nth-child(5) {
    flex: 3;
  }

  .scroll td:nth-child(6) {
    flex: 2;
  }

  .scroll td:nth-child(7) {
    flex: 0.2;
    padding: 0;
  }
</style>

<script>
  (function (document) {
    'use strict';

    var LightTableFilter = (function (Arr) {

      var _input;
      var totalCount;
      var listingCount;

      function _onInputEvent(e) {
        _input = e.target;
        var tables = document.getElementsByClassName(_input.getAttribute('data-table'));
        Arr.forEach.call(tables, function (table) {
          Arr.forEach.call(table.tBodies, function (tbody) {
            Arr.forEach.call(tbody.rows, _filter);
          });
        });

        listingCount = 0;
        Arr.forEach.call(tables, function (table) {
          Arr.forEach.call(table.tBodies, function (tbody) {
            Arr.forEach.call(tbody.rows, _count);
          });
        });
        document.getElementById("listingCount").innerText = listingCount;
      }

      function _count(row) {
        if (row.style.display == '') {
          listingCount++;
        }
      }

      function _filter(row) {
        var text = row.textContent.toLowerCase(), val = _input.value.toLowerCase();
        row.style.display = text.indexOf(val) === -1 ? 'none' : '';
      }

      return {
        init: function () {
          totalCount = parseInt(document.getElementById("listingCount").innerText);
          var input = document.getElementById('light-table-filter-agent');
          if(input)
          input.oninput = _onInputEvent;
        }
      };
    })(Array.prototype);


    LightTableFilter.init();


  })(document);
</script>






















































