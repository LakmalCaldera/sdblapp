version: '2'
services:
  db:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=root
  web:
    image: hpcal1/sdblweb
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db